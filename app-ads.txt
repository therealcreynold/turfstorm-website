# app-ads.txt for turfstorm.com
#
# Advertising networks crawl this file to confirm that whoever is selling advertising in
# Turfstorm is actually allowed to. Without it the better-paying demand simply will not bid,
# and since January 2025 an unverified file means limited serving rather than merely lower
# prices, so this is a launch blocker rather than an optimisation.
#
# This file was deliberately empty of seller lines until 2026-09-02, because Turfstorm
# shipped no advertising SDK and a line for a network you do not serve is a supply-chain
# claim that is untrue. Version 1.0 ships Google AdMob, and alone: no mediation layer, no
# second network. So Google's line is the only one there should be here, and it is the same
# publisher that already sells the other game on this account, because it is the same AdMob
# account.
#
# Serve it from the root of the domain set as the App Store Connect Marketing URL, as
# text/plain, HTTP 200, with no redirect to anywhere else. The crawl walks from the store
# listing to the domain, so it cannot succeed until both exist. Expect the chain to take a
# week or more after launch: store-link availability, then the crawl, then Google's app
# readiness review. Google barely filling in that window is the system working, not a bug.
#
# Add a network's lines only when that network is actually enabled, and paste what its own
# dashboard gives you rather than typing it. A wrong line is worse than a missing one,
# because it looks verified and is not.

# The IAB variable naming the business that owns this inventory. It has to be the domain set
# as the App Store Connect Marketing URL, because that link is the path every crawler walks
# to get here.
OWNERDOMAIN=turfstorm.com

google.com, pub-5706069574815967, DIRECT, f08c47fec0942fa0
